/** BullMQ queue that carries due reminders to the delivery worker. */
export const REMINDER_QUEUE = 'reminders';

/** Job name for a single reminder delivery. */
export const DELIVER_JOB = 'deliver';
