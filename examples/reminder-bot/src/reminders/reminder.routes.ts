/**
 * Callback route templates, shared by the router (`@Action`) and the keyboards
 * that build the buttons — one source of truth so the two never drift.
 */
export const DONE_ROUTE = 'done/:id';
export const DELETE_ROUTE = 'del/:id';
